# <краткое описание изменения>

<!--
Шаблон Merge Request. Заполнять по делу: ревьюер — это будущий ты сам или агент-ревьюер.
Правила — AGENTS.md (§7 цикл задачи, §9 Definition of Done, §13 git-конвенции).
-->

## Что и зачем

<!-- 1–3 предложения: какую проблему решает изменение. Не пересказывать дифф. -->

## Источник задачи

- Постановка: `ai-tasks/<номер>-<задача>/<номер>-<микро-задача>.md`
- Отчёт исполнителя: `ai-tasks/<задача>/<микро-задача>_report_v<версия>.md`
- Ревью: `ai-tasks/<задача>/<микро-задача>.review.md`

<!-- Если MR не по постановке (срочная правка, инфраструктура) — написать «вне постановки» и причину. -->

## Тип изменения

- [ ] `feat` — новая функциональность
- [ ] `fix` — исправление дефекта
- [ ] `refactor` — без изменения поведения
- [ ] `test` — только тесты
- [ ] `docs` — документация и правила
- [ ] `chore` — инфраструктура, CI, зависимости

## Затронутые слои

- [ ] `backend/Domain` · [ ] `backend/Core` · [ ] `backend/Contracts`
- [ ] `backend/Infrastructure.*` · [ ] `backend/Web`
- [ ] `frontend/apps/*` · [ ] `frontend/packages/*`
- [ ] `docker/` · [ ] `ci/` · [ ] `docs/`

## Чек-лист Definition of Done

- [ ] Код компилируется без предупреждений (`dotnet build`, `pnpm build`).
- [ ] Статанализ чистый (`dotnet format --verify-no-changes`, `pnpm lint`, `pnpm typecheck`).
- [ ] На новый функционал добавлены тесты нужного уровня.
- [ ] **Все** тесты зелёные, включая ранее существовавшие.
- [ ] Архитектурные тесты проходят (границы слоёв не нарушены).
- [ ] Контракты и документация актуальны (`ARCHITECTURE.md`, `docs/`, OpenAPI-схема).
- [ ] Секретов в коде, конфигах и логах нет.
- [ ] Frontend: реализованы состояния загрузка/пусто/ошибка; a11y-чек-лист пройден.

## Проверка

<!-- Как проверено: команды, сценарии, что смотрел вручную. -->

```
dotnet build && dotnet test --solution backend/MarketSniper.slnx -c Debug
pnpm lint && pnpm typecheck && pnpm test
```

## Не тестировалось

<!-- Что сознательно не проверялось и почему: нет окружения, данные недоступны, отложено.
     Если проверено всё — написать «не тестировалось: ничего», а не оставлять пустым. -->

## Решения за владельцем

<!-- Что требует решения пользователя: продуктовые и визуальные вопросы, необратимые ADR,
     схемы тестирования, публикации, секреты, внешние записи. -->

## Риски и откат

- Риск: <!-- что может сломаться -->
- Откат: <!-- revert MR / откат миграции / переключение образа на предыдущий тег -->

## Скриншоты / ответы API

<!-- Для UI-изменений — до/после; для API — пример запроса и ответа. -->

## Что НЕ входит в этот MR

<!-- Явно перечислить, чтобы ревьюер не искал «недостающее». -->
