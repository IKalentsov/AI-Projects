# Отчёт: <название-микро-задачи>

**Постановка:** `ai-tasks/<задача>/<номер>-<микро-задача>.md`
**Версия отчёта:** v1
**Дата:** <ГГГГ-ММ-ДД>
**Исполнитель:** <модель/инструмент>

---

## 1. Что сделано

<По пунктам постановки: «требовалось → сделано». Без воды.>

## 2. Изменённые файлы

| Файл | Действие | Комментарий |
|------|----------|-------------|
| `path/to/file.cs` | создан / изменён | что именно |

## 3. Тесты

| Уровень | Добавлено | Команда | Результат |
|---------|-----------|---------|-----------|
| unit | N тестов | `dotnet test --filter ...` | зелёные |
| integration | N тестов | `dotnet test` | зелёные |

Итог полного прогона: `<команда>` → `<всего/пройдено/упало>`.

## 4. Отклонения от постановки

<Всё, что сделано иначе, чем в постановке, и почему. Если отклонений нет — написать «нет».>

## 5. Проблемы и вопросы

<Что не получилось, где не хватило контекста, какие решения требуют подтверждения.>

## 6. Самопроверка по Definition of Done

- [ ] Код компилируется без предупреждений.
- [ ] Статанализ и линтер чистые.
- [ ] Тесты на новый функционал добавлены.
- [ ] **Все** тесты зелёные (включая существующие).
- [ ] Секретов в коде нет.
- [ ] Файлы `ARCHITECTURE.md`, `AGENTS.md`, `docs/**`, `ai-tasks/**` не изменялись
      (кроме этого отчёта).
