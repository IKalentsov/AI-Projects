# Ревью: <название-микро-задачи>

**Постановка:** `ai-tasks/<задача>/<номер>-<микро-задача>.md`
**Отчёт исполнителя:** `<микро-задача>_report_<версия>.md`
**Дата:** <ГГГГ-ММ-ДД>
**Ревьюер:** архитектор

## Вердикт

**ОК** / **Доработки** — <одна строка: почему>

## Прогоны

| Проверка | Команда | Результат |
|----------|---------|-----------|
| сборка | `dotnet build` | |
| unit-тесты | `dotnet test` | |
| integration-тесты | `dotnet test` | |
| статанализ | `dotnet format --verify-no-changes` | |
| frontend build/typecheck | `pnpm build`, `pnpm typecheck` | |
| frontend тесты | `pnpm test` | |
| git | `git status`, `git diff` | <не тронуты ли .md-артефакты> |

## Чекпоинты «требовалось → сделано»

| # | Требование постановки | Факт (файл) | Статус |
|---|-----------------------|-------------|--------|
| 1 | | | ✅ / ⚠️ / ❌ |
| 2 | | | |

## Замечания и их закрытие

> Каждое замечание получает **ровно одно** решение: `fix` (исправить в этой задаче),
> `defer` (валидно, но вне объёма → отдельная задача в `ai-tasks/`),
> `reject` (отклонено с обоснованием). Замечание без решения = задача не закрыта
> (`WORKFLOW.md` §5).

| # | Замечание (файл:строка) | Важность | Решение | Куда |
|---|-------------------------|----------|---------|------|
| 1 | | блокирующее / незначительное | `fix` / `defer` / `reject` | `fixes.md` / задача `ai-tasks/NN-...` / обоснование ниже |

### Обоснования `reject`

1. <замечание → факт, правило или ссылка, почему отклонено>

### Отложено (`defer`)

1. <замечание → файл задачи, куда отложено>

## Проверка качества кода

- [ ] Слои и направление зависимостей не нарушены.
- [ ] Паттерны проекта соблюдены (Result, Value Objects, snake_case, тонкие контроллеры).
- [ ] Нет дублей, magic strings, «абстракций на будущее».
- [ ] Обработка ошибок явная, без проглоченных исключений.
- [ ] Производительность: нет N+1, есть пагинация, индексы, `AsNoTracking`.
- [ ] Безопасность: нет секретов, вход валидируется, внешние вызовы с таймаутами.
- [ ] Тесты проверяют поведение и детерминированы.
- [ ] Фронтенд: состояния (загрузка/пусто/ошибка), a11y, нет `any`, нет ручного CSS.

## Что дальше

<Следующий шаг: fixes-файл, доработка постановки, приёмка пользователем.>
