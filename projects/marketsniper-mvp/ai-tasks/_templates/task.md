# <номер>-<название-микро-задачи>

**Требует изменения архитектуры:** да / нет
**Статус:** черновик
**Задача:** <номер>-<название-задачи>
**Зависит от:** <другие микро-задачи или «нет»>
**Слой / приложение:** <backend: Domain | Core | Infrastructure | Web | tests / frontend: feature | package>

---

## 1. Цель

<Что должно получиться. 1–3 предложения, измеримо. Без «сделать красиво».>

## 2. Контекст

<Где проект, какой стек, что уже реализовано, как связаны существующие файлы.
Исполнитель не видит наш диалог — здесь весь нужный контекст.>
<Ссылки: `ARCHITECTURE.md`, `backend/AGENTS.md`, `frontend/AGENTS.md`, релевантные скиллы.>

## 3. Файлы

**Создать:**

- `path/to/NewFile.cs` — назначение

**Изменить:**

- `path/to/Existing.cs` — что именно меняется

**Не трогать:** <перечислить явно>

## 4. Шаги

1. <шаг>
2. <шаг>
3. <шаг>

## 5. Best practice / примеры

<Псевдокод, сигнатуры, паттерны проекта. Ссылки на официальную документацию.
Только там, где есть риск ошибиться.>

```csharp
// Пример: фабрика агрегата возвращает Result<T>
public static Result<Product> Create(ProductId id, ProductName name /* ... */)
{
    // ...
}
```

## 6. Тесты

| Уровень | Проект | Что покрыть |
|---------|--------|-------------|
| unit | `MarketSniper.UnitTests` | <сценарии> |
| integration | `MarketSniper.IntegrationTests` | <эндпоинты/БД, фикстуры> |
| architecture | `MarketSniper.ArchitectureTests` | <если меняются зависимости> |
| frontend unit/component | `apps/web` (Vitest + RTL) | <сценарии> |
| frontend e2e | Playwright | <ключевой сценарий> |

Сценарии, которые обязательно должны быть покрыты:

- <позитивный>
- <негативный / граничный>

## 7. Критерии приёмки

- [ ] <код собирается: команда + ожидаемый результат>
- [ ] <тесты: команда + все зелёные>
- [ ] <статанализ: `dotnet format --verify-no-changes` / `pnpm lint` — чисто>
- [ ] <функциональный критерий: что можно проверить руками/тестом>
- [ ] <документация: что обновлено (если применимо)>

## 8. Условие остановки

**Единственное условие остановки:** <наблюдаемый результат, при достижении которого исполнитель
останавливается и пишет отчёт. Формулировка проверяемая, например: «эндпоинт `POST /api/v1/searches`
возвращает 200 с непустым списком и все тесты зелёные»>.

- Достигнуто → остановиться, написать отчёт, **объём не расширять**.
- Обнаружено «заодно» (соседний баг, рефакторинг, тест вне объёма) → в раздел
  «Отклонения и замечено» отчёта, но **не в код**.
- Условие недостижимо по внешней причине (нет данных, конфликт с `ARCHITECTURE.md`) →
  остановиться **раньше** и сообщить, а не импровизировать.

Подробно — `WORKFLOW.md` §3.

## 9. Что НЕ трогать

- `ARCHITECTURE.md`, `AGENTS.md`, `WORKFLOW.md`, `docs/**`, `ai-tasks/**` (кроме своего отчёта).
- <прочие файлы/слои/пакеты>

## 10. Порядок сдачи

1. Запустить сборку, тесты и статанализ (см. §7).
2. Написать отчёт: `ai-tasks/<задача>/<микро-задача>_report_v1.md` по шаблону
   `ai-tasks/_templates/report.md`.
3. Приложить список изменённых файлов и результаты прогонов.
