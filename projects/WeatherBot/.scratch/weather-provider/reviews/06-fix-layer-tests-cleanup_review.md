# Ревью: тикет 06 — Приборка после ревью 04–05

**Тикет:** `.scratch/weather-provider/issues/06-fix-layer-tests-cleanup.md`
**Спецификация:** `.scratch/weather-provider/spec.md`
**Отчёт исполнителя:** `reports/06-fix-layer-tests-cleanup_report.md`
**Дата:** 2026-09-22
**Ревьюер:** архитектор

## Вердикт

**ОК — принято без доработок.** Reflection-обвязка убрана, негативная проверка вызывает метод
напрямую, канарейка проверяет обе пары, `AT-01` назван по семантике «использует». Тронут ровно
один файл (`LayerDependenciesTests.cs`, 05:21); `src`, `*.csproj` и `Directory.Packages.props` —
без изменений. Сборка `Ошибок: 0` / `Предупреждений: 0`, `UnitTests` — `122, сбой: 0`,
`ArchitectureTests` — `13, сбой: 0`.

Отдельно отмечаю: исполнитель **не выполнил мою инструкцию и был прав**. Тикет требовал убрать
`using System.Reflection`, но в этом пространстве имён живут `Assembly` и `AssemblyName`, которыми
файл пользуется, — без `using` сборка не собралась бы. Исполнитель оставил его и объяснил в отчёте
(§1, п. 4). Это первый случай, когда он возразил по существу вместо слепого исполнения, — ровно то
поведение, которого мы хотим. Ошибка в тикете была моя.

## Прогоны (мои, не из отчёта)

| Проверка | Команда | Результат |
|----------|---------|-----------|
| сборка | `dotnet build WeatherBot.slnx -c Debug -m:1 --no-restore -p:NuGetAudit=false` | `Ошибок: 0`, `Предупреждений: 0` ✅ |
| unit-тесты | `dotnet tests/WeatherBot.UnitTests/bin/Debug/net10.0/WeatherBot.UnitTests.dll` | `всего: 122, сбой: 0`, `684 ms` ✅ |
| архитектурные | `dotnet .../WeatherBot.ArchitectureTests.dll` | `всего: 13, сбой: 0`, `268 ms` ✅ |
| объём правки | времена файлов: изменён только `LayerDependenciesTests.cs` (05:21) | ✅ |

## Чекпоинты «требовалось → сделано»

| # | Требование | Факт | Статус |
|---|-----------|------|--------|
| 1 | Негативная проверка вызывает `AssertNoDependencyOn` напрямую | `Assert.Throws<Xunit.Sdk.XunitException>(() => AssertNoDependencyOn(fakeDeps, "FakeLayer", "WeatherBot.Domain"))`; `GetMethod`, `Invoke`, `TargetInvocationException`, `method!`, `BindingFlags` убраны; проверка текста сообщения сохранена | ✅ |
| 2 | Канарейка дополнена второй парой | `Application → Domain` и `Infrastructure → Application` в одном тесте | ✅ |
| 3 | `AT01` переименован по семантике «использует» | `AT01_Domain_usesNoOtherProjectAssembly`; doc-комментарий класса уже описывал семантику «не использует» | ✅ |
| 4 | Сборка `0/0` | подтверждено | ✅ |
| 5 | `UnitTests` — 122, `ArchitectureTests` — не меньше 13 | 122 и 13 | ✅ |
| 6 | `src/**`, `*.csproj`, `Directory.Packages.props` не изменены | подтверждено временами и `git status` | ✅ |

## Замечания и их закрытие

| # | Замечание | Важность | Решение | Куда |
|---|-----------|----------|---------|------|
| 1 | Вывод команд в отчёте выглядит **перенабранным**, а не скопированным: в строке архитектурных тестов путь содержит прямой слэш (`net10.0/WeatherBot.ArchitectureTests.dll`), в обеих сводках отсутствует строка `длительность`, которую печатает раннер, а дерево сборки приведено с полными путями. Содержание верное — числа и исход сходятся с моим прогоном — но требование «дословно» формально не выполнено | незначительное | `defer`: отдельный тикет не нужен, содержимое подтверждено моим прогоном. В следующих тикетах формулировка остаётся: вывод вставляется как есть, вместе со строкой `длительность` | — |
| 2 | Раздел «Отклонения» отчёта говорит «Нет», хотя отклонение было — оставленный `using System.Reflection`. Оно объяснено в §1, но не вынесено в раздел отклонений | незначительное | наблюдение: поведение правильное, оформление — нет. Раздел отклонений существует именно для таких мест | — |
| 3 | Моя инструкция «убрать `using System.Reflection`» была ошибочной | — | принято как ошибку архитектора; в отчёте исполнителя исправлять нечего | — |

### Обоснования `reject`

Претензий к исполнителю нет. Наоборот: пункт 3 — ошибка в тикете, которую исполнитель распознал
и корректно обошёл.

### Отложено (`defer`)

1. Форма подачи вывода команд (замечание 1) — без отдельного тикета, требование действует дальше.

## Проверка качества кода

- [x] Reflection убран полностью — прямая типизированная проверка, ошибка переименования ловится
      компилятором, а не в рантайме.
- [x] Проверка текста сообщения исключения сохранена — тест не ослабел.
- [x] Канарейка доказывает и загрузку сборок, и непустоту наборов на двух парах.
- [x] Имена правил и doc-комментарий согласованы по семантике «использует».
- [x] Объём правки — ровно один файл, лишнего нет.
- [x] Регрессий нет: 122 unit + 13 архитектурных.

## Оценка отчёта исполнителя

Отчёт точный по содержанию: все три пункта, включая два необязательных, выполнены и описаны верно;
объяснение про `using System.Reflection` — самостоятельное и корректное. К оформлению две
незначительные претензии: перенабранный вывод команд и отсутствие записи об отклонении в
соответствующем разделе.

## Что дальше

1. Тикет `06` — `ready-for-human`: сдан, ждёт просмотра и коммита.
2. Фича `.scratch/weather-provider` закрыта полностью: тикеты `01`–`06` сданы.
3. Открытый реестр по проекту: `F-02`, `F-06`, `F-08`–`F-11`, `F-13`, `F-16`, `F-17` и вопрос
   о неиспользуемых пакетах ArchUnitNET.
