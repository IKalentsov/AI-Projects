# 03: Граничные дефекты маппера и форматтера

**What to build:** Три известных граничных дефекта закрыты вместе с тестами, которые падали
до правки: часовой пояс с получасовым и сорокапятиминутным смещением отображается верно,
отрицательное значение осадков не превращается в «очень сильные», а влажность округляется,
а не усекается.

Дефекты зарегистрированы как `F-14` в `ARCHITECTURE.md`. Точные места и ожидаемое поведение —
`spec.md` §4, тикет 03.

**Blocked by:** None (can start immediately)

**Status:** ready-for-human — сдано и принято ревью, ждёт просмотра и коммита пользователя

- [x] Сборка: `Ошибок: 0`, `Предупреждений: 0`
- [x] Тест на смещение `:30` и `:45` падает до правки и проходит после; смещение без минут
      по-прежнему печатается без них
- [x] Тест на отрицательную `precipitation` падает до правки и проходит после
- [x] Тест на округление влажности падает до правки и проходит после
- [x] `git status --short -- src` показывает ровно три исправленных файла
      (плюс ранее принятый `OpenMeteoContracts.cs` из тикета 01 — дерево ещё не закоммичено)
- [x] Все ранее написанные тесты остаются зелёными (122 = 116 + 6)
- [x] Отчёт `reports/03-mapper-formatter-boundaries_report.md` с дословным выводом команд

## Comments

**2026-09-22, ревьюер.** Ревью — `reviews/03-mapper-formatter-boundaries_review.md`. Вердикт:
**ОК, принято без доработок.** Мои прогоны: сборка `0/0`, `122` unit-теста, `11` архитектурных,
сбой `0`; новых подавлений анализаторов нет. Падение тестов на старом коде проверил расчётом
старых выражений: `+5:30 → UTC+5`, `+5:45 → UTC+5`, `-1.0 → VeryHeavy`, `72.6 → 72` — все четыре
регрессионных теста действительно ловили дефекты. Заявленное в отчёте согласование с
`ToPressureMmHg` подтвердилось. Находка `F-14` закрыта и вычеркнута из открытого реестра.
Отложено: тест на отрицательное смещение с минутами (ушёл в тикет 04) и режим округления
температуры (`F-17`).
