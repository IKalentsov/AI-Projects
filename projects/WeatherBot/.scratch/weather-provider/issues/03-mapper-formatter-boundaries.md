# 03: Граничные дефекты маппера и форматтера

**What to build:** Три известных граничных дефекта закрыты вместе с тестами, которые падали
до правки: часовой пояс с получасовым и сорокапятиминутным смещением отображается верно,
отрицательное значение осадков не превращается в «очень сильные», а влажность округляется,
а не усекается.

Дефекты зарегистрированы как `F-14` в `ARCHITECTURE.md`. Точные места и ожидаемое поведение —
`spec.md` §4, тикет 03.

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] Сборка: `Ошибок: 0`, `Предупреждений: 0`
- [ ] Тест на смещение `:30` и `:45` падает до правки и проходит после; смещение без минут
      по-прежнему печатается без них
- [ ] Тест на отрицательную `precipitation` падает до правки и проходит после
- [ ] Тест на округление влажности падает до правки и проходит после
- [ ] `git status --short -- src` показывает ровно три исправленных файла
      (плюс ранее принятый `WeatherBot.Infrastructure.csproj`)
- [ ] Все ранее написанные тесты остаются зелёными
- [ ] Отчёт `reports/03-mapper-formatter-boundaries_report.md` с дословным выводом команд
