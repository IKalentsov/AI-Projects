# 02: Тесты состояния, буфера журнала, маскировки секретов и направлений зависимостей

**What to build:** Состояние бота, кольцевой буфер журнала и маскировка секретов проверены
на границах и под параллельной нагрузкой, а направление зависимостей между слоями закреплено
исполняемым архитектурным тестом, который падает при нарушении. После тикета тестовый контур
покрывает всё решение, а не только маппер и форматтер.

Детали контрактов — `spec.md` §4, тикет 02.

**Blocked by:** None (can start immediately) — тикет 01 сдан: сборка `0/0`, 89 + 6 тестов зелёные;
остаточные доработки вынесены в тикет 04 и этого тикета не касаются

**Status:** ready-for-human — сдано; приёмка пройдена, доработки вынесены в тикет 05

- [x] Сборка: `Ошибок: 0`, `Предупреждений: 0`
- [x] Обе тестовые сборки: `сбой: 0`; число пройденных включает тесты тикета 01 (116 и 11)
- [x] `BotStateManager`: стартовое состояние, `Start`/`Stop`, `MarkSent`/`MarkNextTick`,
      многопоточность без исключений и потери состояния
- [x] `InMemoryLogBuffer`: предел `Capacity`, вытеснение старой записи, порядок `GetRecent(n)`,
      `GetRecent(0)` и отрицательное значение, отказ на пустой строке
- [x] `SecretMasker`: одно и несколько вхождений, пустой/`null` секрет, пустой/`null` текст
- [ ] Архитектурный тест проверяет все пять правил направления зависимостей и **падает при
      нарушении**; в отчёте сказано, какое правило каким тестом проверяется — правила реализованы,
      но падение при нарушении не продемонстрировано, а два правила описывают не тот факт, что
      проверяют → тикет 05
- [x] `src/**` не изменён
- [x] Отчёт `reports/02-tests-state-buffer-layers_report.md` с дословным выводом команд

## Comments

**2026-09-22, ревьюер.** Ревью — `reviews/02-tests-state-buffer-layers_review.md`. Вердикт:
**ОК после доработок**. Мои прогоны: сборка `0/0`, `116` unit-тестов, `11` архитектурных, сбой `0`,
`src/**` не тронут. Юнит-часть принята как есть. Главное замечание — архитектурные тесты:
`Assembly.GetReferencedAssemblies()` видит только **использованные** ссылки, поэтому правила
`AT-03`/`AT-05` проходят из-за выброшенных компилятором неиспользуемых ссылок (`F-10`, `F-16`),
а не потому, что граф проектов такой; падение при нарушении не продемонстрировано. Доработки —
тикет `05-fix-layer-tests`.
