# WORKFLOW.md — процесс работы над WeatherBot

> **Когда читать**: перед написанием спецификации и тикетов, при ревью, при сдаче задачи.
> В остальное время достаточно `AGENTS.md`.
>
> **Иерархия правил**: все правила агента (роли, конвейер, DoD, язык, git-границы, скиллы) —
> в `AGENTS.md` этого проекта; устройство системы — в `ARCHITECTURE.md`. Здесь —
> **только процессные отличия проекта**, без пересказа правил.

**Последнее обновление:** 2026-09-22

---

## 1. Раскладка проекта

```
projects/WeatherBot/                     # корень проекта: документация, процесс, артефакты фич
├── README.md                            # обзор, API, запуск
├── ARCHITECTURE.md                      # структура, контракты, стек — единый источник правды
├── WORKFLOW.md                          # этот файл
├── AGENTS.md                            # правила агента
├── docs/agents/                          # конфиг инженерных скиллов: трекер, домен, триаж
├── .scratch/                            # фичи: spec.md + issues/ + reports/ + reviews/
└── backend/
    └── WeatherBot/                      # каталог решения
        ├── WeatherBot.slnx              # решение (5 проектов)
        └── src/
            ├── WeatherBot.Contracts/    # DTO наружу
            ├── WeatherBot.Domain/       # модели и настройки — только BCL
            ├── WeatherBot.Application/  # абстракции + WeatherDigestService
            ├── WeatherBot.Infrastructure/ # реализации интеграций, фоновая задача
            └── WeatherBot.Web/          # REST API: Program.cs, Controllers/, DI
```

- Код — только в `backend/WeatherBot/src/…`; документы — в корне проекта.
- Тестовые проекты: `backend/WeatherBot/tests/WeatherBot.UnitTests` и
  `…/tests/WeatherBot.ArchitectureTests`. **IntegrationTests не заводятся** — внешние швы
  подменяются, поднимать нечего (обоснование — `ARCHITECTURE.md`, §Тестирование).

## 2. Команды

Каталог — `backend/WeatherBot`.

```bash
dotnet restore WeatherBot.slnx -m:1 -p:NuGetAudit=false      # офлайн из локального кэша
dotnet build WeatherBot.slnx -c Debug -m:1 --no-restore -p:NuGetAudit=false   # сборка
dotnet tests/WeatherBot.UnitTests/bin/Debug/net10.0/WeatherBot.UnitTests.dll
dotnet tests/WeatherBot.ArchitectureTests/bin/Debug/net10.0/WeatherBot.ArchitectureTests.dll
dotnet run --project src/WeatherBot.Web                       # запуск — ТОЛЬКО пользователь
```

**Ограничения песочницы агента** — коротко (проверено прогонами 2026-09-21 и 2026-09-22):

- `-m:1` обязателен **и для `restore`, и для `build`**: без него команда завершается кодом 1,
  не напечатав ни строки, а сводка врёт — `Ошибок: 0`;
- `-p:NuGetAudit=false` обязателен: без сети `NU1900` становится ошибкой из-за `TreatWarningsAsErrors`;
- `dotnet test` на MTP падает на именованном канале — тесты запускаются **прямым вызовом
  собранной сборки**;
- **restore доступен агенту** (офлайн из кэша) и пользователю. Пользователь нужен только за
  пакетами, которых в локальном кэше нет.

**Как выглядит, если про это забыть** (одинаково для любого .NET-проекта в харнессе):

- команда вернула `Ошибок: 0`, но код 1 и пустой вывод → не хватает `-m:1`;
- `NU1900` в restore или сборке → не хватает `-p:NuGetAudit=false`;
- тест-хост MTP упал на `NamedPipeClientStream` → запускать собранную сборку напрямую,
  а не `dotnet test`.

**Это обходы только для агента.** На машине пользователя и в CI работают штатные
`dotnet restore` / `dotnet build` / `dotnet test`.

- Тестовый контур создан задачей `01-03`: в `UnitTests` — тесты маппера и форматтера (68 штук),
  `ArchitectureTests` пока пуст и наполняется тикетами `01`–`02` фичи `weather-provider`.

## 3. Стек и запреты проекта

- .NET 10, последняя версия C#, Clean Architecture (5 проектов), Result Pattern.
- **БД нет** — EF Core, Npgsql, Dapper, миграции не добавляются.
- **Docker не используется** — хостинг локальный, на компьютере пользователя.
- **Фронтенда в решении нет** — UI планируется отдельным SPA на React поверх REST API.
- Версии пакетов — только в `backend/Directory.Packages.props`.
- Анализаторы: `TreatWarningsAsErrors=true`. Новое отключение правила — только с согласия
  пользователя, с комментарием-причиной в `backend/.globalconfig` и записью в `ARCHITECTURE.md`.

## 4. Цикл работы

Конвейер, роли, правила тикета и ревью — в `AGENTS.md` (§4–§6) и не дублируются здесь.
Проектные отличия два:

- **артефакты фичи лежат в `.scratch/<feature-slug>/`** этого проекта — `spec.md`,
  `issues/<NN>-<slug>.md`, `reports/`, `reviews/`. Из рабочей области не выходим;
- **коммит делает пользователь.** Конвейер Мэта Покока завершает `/implement` и `/code-review`
  коммитом; у нас агент останавливается на готовых файлах и предлагает текст коммита.

Порядок для фичи `weather-provider` (`spec.md` §2): тикет `01` → ревью → тикет `02`;
тикет `03` независим и может идти в любой момент.

## 5. Git-конвенции проекта

- **Git-операции выполняет только пользователь.** Агенту разрешено чтение: `git status`,
  `git diff`, `git log`, `git show`, `git blame`. Ветку создаёт и push делает пользователь.
- Ветки: `develop` — рабочая; `feature/<краткое-имя>`, `fix/<краткое-имя>`, `chore/<...>`.
- Коммиты — Conventional Commits: `feat(backend): …`, `fix(web): …`, `test(core): …`,
  `docs: …`, `chore: …`. Тип и область — английские, текст — русский.
- В теле коммита — «почему», а не пересказ диффа. Трейлеры `Refs:` (номер тикета и путь
  спецификации, например `Refs: .scratch/weather-provider/issues/01-…`) и `Not tested:`
  (что не проверялось) — по необходимости.
- Не коммитить сгенерированное (`bin/`, `obj/`, `.vs/`) и секреты:
  `appsettings.Development.json` закрыт правилом в `backend/.gitignore`.
- Текст коммита агент готовит по фактическому `git status` / `git diff` (`AGENTS.md` §6),
  но сам не выполняет ни `add`, ни `commit`, ни `push`.

## 6. Отклонения от глобальных правил

Раздел-заглушка: сюда попадает **осознанное** отключение архитектурного или процессного
требования для этого проекта — с причиной и решением пользователя. Формат записи:

> `ГГГГ-ММ-ДД` — отключено требование «…» по решению пользователя: причина, объём, условие возврата.

> `2026-09-21` — отключено требование DoD «тесты пишутся вместе с кодом» по решению пользователя.
> Причина: приоритет — полностью убрать из проекта платный источник погоды и перейти на
> Open-Meteo; тестовых проектов в решении ещё нет, и заводить их одновременно со сменой
> провайдера — лишний риск.
> Объём: задача `01-01-remove-yandex` — код без тестов.
> Условие возврата: **выполнено** — тестовый контур заведён задачей `01-03`, тикет `01`
> закрывает провайдера, оркестрацию и DI.

> `2026-09-22` — процесс переведён на конвейер Matt Pocock по решению пользователя.
> Причина: прежний цикл `ai-tasks/` (постановка → отчёт → review → fixes) дублировал то, что
> конвейер делает из коробки, и требовал ручной поддержки статусов.
> Объём: артефакты переехали в `.scratch/<фича>/` (`spec.md`, `issues/`, `reports/`, `reviews/`),
> конфиг скиллов — `docs/agents/`; правила агента переписаны (`AGENTS.md` §4–§6); папка
> `ai-tasks/` выводится из работы.
> Отклонение от раскладки по умолчанию: `CONTEXT.md` и `docs/adr/` **не заводятся** —
> доменная документация остаётся в `ARCHITECTURE.md` (`docs/agents/domain.md`).
