# Отчёт: <название-микро-задачи>

**Постановка:** `ai-tasks/<задача>/<номер>-<микро-задача>.md`
**Версия отчёта:** v1
**Дата:** <ГГГГ-ММ-ДД>
**Исполнитель:** <модель / инструмент>

---

## 1. Что сделано

<По пунктам постановки: «требовалось → сделано». Без воды.>

## 2. Изменённые файлы

<Полные пути от корня проекта.>

| Файл | Действие | Комментарий |
|------|----------|-------------|
| `backend/WeatherBot/src/...` | создан / изменён | что именно |

## 3. Команды и дословный вывод

**Сборка:**

```
<команда>
<дословный вывод: Ошибок: 0, Предупреждений: 0>
```

**Тесты** (если тестовые проекты существуют; иначе написать прямо, что их нет):

```
<команда>
<дословный вывод: всего / сбой / успешно>
```

Пересказ вывода доказательством не считается. Если проверка выполнялась инструментом без
вывода (`write`/`read`) — указать, чем именно проверялось.

## 4. Что не проверено и почему

<Например: живой вызов внешнего API невозможен без ключа; проект запускает только пользователь.>

## 5. Отклонения от постановки и замечено «заодно»

<Всё, что сделано иначе, чем в постановке, и почему. Что замечено, но не делалось.
Если ничего — написать «нет».>

## 6. Проблемы и вопросы

<Что не получилось, где не хватило контекста, какие решения требуют подтверждения.>

## 7. Самопроверка по Definition of Done

- [ ] Сборка: `Ошибок: 0`, `Предупреждений: 0`.
- [ ] Тесты на новый функционал добавлены (если функционал есть); все зелёные.
- [ ] Секретов в коде и отчёте нет.
- [ ] Файлы `ARCHITECTURE.md`, `AGENTS.md`, `WORKFLOW.md`, `memory-bank/**`, `ai-tasks/**`
      не изменялись (кроме этого отчёта).
- [ ] Условие остановки из постановки достигнуто, объём не расширен.
