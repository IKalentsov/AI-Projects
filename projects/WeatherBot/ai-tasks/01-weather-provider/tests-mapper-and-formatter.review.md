# Ревью: 01-03-tests-mapper-and-formatter

**Постановка:** `ai-tasks/01-weather-provider/01-03-tests-mapper-and-formatter.md`
**Отчёт исполнителя:** `tests-mapper-and-formatter_report_v1.md`
**Дата:** 2026-09-22
**Ревьюер:** архитектор

## Вердикт

**ОК (после доработок).** Приёмка пройдена и **перепроверена мной**: сборка `Ошибок: 0` /
`Предупреждений: 0`, `WeatherBot.UnitTests.dll` — **68 пройдено, 0 сбоев**. Тесты написаны
содержательно, границы покрыты. Следов порчи файлов нет — правило про запрет shell-правки
сработало. Три замечания закрыты во втором проходе (`tests-mapper-and-formatter_report_v2.md`).

**`NoWarn S4144` — вопрос закрыт: отключение отклонено пользователем, дубликаты устранены
архитектором** (см. «Что дальше»). Новых отключений анализаторов в проекте не появилось.

## Прогоны (мои, не из отчёта)

| Проверка | Команда | Результат |
|----------|---------|-----------|
| сборка | `dotnet build WeatherBot.slnx -c Debug -m:1 --no-restore -p:NuGetAudit=false` | `Ошибок: 0`, `Предупреждений: 0` ✅ |
| тесты | `dotnet tests/WeatherBot.UnitTests/bin/Debug/net10.0/WeatherBot.UnitTests.dll` | `всего: 68, сбой: 0, успешно: 68` ✅ |
| маркеры порчи | поиск `[fact]`, `[theory]`, `[case(`, `using Xunit.v3`, литерального `\r\n` | **ни одного совпадения** ✅ |
| `global.json` | существование и содержимое | создан ✅ |

## Чекпоинты «требовалось → сделано»

| # | Требование постановки | Факт | Статус |
|---|-----------------------|------|--------|
| 1 | Удалить девять невалидных файлов | в `tests/` остались только два новых файла тестов | ✅ |
| 2 | Создать `backend/WeatherBot/global.json` | создан с `test.runner = Microsoft.Testing.Platform` | ✅ |
| 3 | Сборка после каждого файла | в отчёте подтверждено тремя сборками; результат сходится | ✅ |
| 4 | Тесты маппера: все границы контракта | давление, 8 румбов + границы 23/337, пороги облачности по обе стороны, коды WMO, `Mixed`, нулевые осадки, интенсивность по обе стороны границ, `TryParseObservedAt` (в т.ч. `null`, пусто, пробелы, отрицательное смещение) | ✅ |
| 5 | Тесты форматтера: город, без «МСК», смещение, `Unknown`, осадки | два разных города, положительное и отрицательное смещение, `Нет данных`, `—`, отсутствие скобки у `None`/`Unknown`, скобка у `Rain`/`Snow` | ✅ |
| 6 | `ArchitectureTests` пуст → код 8 ожидаем | подтверждено, отмечено в отчёте | ✅ |
| 7 | Секретов нет | `appsettings.Development.json` не тронут | ✅ |
| 8 | Приёмка приведена дословно | вывод команд есть, включая телеметрию и `Exit code: 8` | ✅ |

## Замечания и их закрытие

| # | Замечание (файл) | Важность | Решение | Куда |
|---|------------------|----------|---------|------|
| 1 | Добавлена прямая зависимость `xunit.v3.extensibility.core` (в `Directory.Packages.props` и `WeatherBot.UnitTests.csproj`) и как следствие все атрибуты пишутся полностью квалифицированно — `[Xunit.Fact]`. Обоснование в отчёте («`xunit.v3` не даёт атрибутов, `using Xunit;` не резолвится») противоречит проверенному факту: в пробном проекте на этом же SDK 10 с одной ссылкой `xunit.v3` 4.0.0 код с `using Xunit;` и `[Fact]` **собирался и проходил**. Похоже на неверный диагноз на фоне прошлых ошибок | незначительное | `fix` | `tests-mapper-and-formatter.fixes.md`, правка 1 |
| 2 | `backend/Directory.Packages.props`, `tests/Directory.Build.props`, `WeatherBot.UnitTests.csproj` изменены, хотя постановка §3 прямо запрещала их трогать. Исполнитель **честно указал это в отчёте** (§5), не скрыл | незначительное | `fix` | правка 1 (пакеты) + решение пользователя по `S4144` |
| 3 | Assertions вида `.Should().Contain("*Погода в Москва*", "OrdinalIgnoreCase")` — второй аргумент у `Contain` это `because` (текст сообщения), а не режим сравнения. Сравнение фактически регистрозависимое, а «OrdinalIgnoreCase» попадает в текст ошибки. Тесты проходят, потому что значения совпадают точно | незначительное | `fix` | правка 2 |
| 4 | Отчёт §1 утверждает «`git status --short -- src` пуст». Фактически там строка `WeatherBot.Infrastructure.csproj` — это `InternalsVisibleTo`, добавленный в `01-01` и **принятый**. К изменению `src` в этой задаче отношения не имеет, но заявление неточно. Причина — плохо сформулированный критерий в постановке (моя вина) | незначительное | `reject` (в адрес исполнителя) | критерий исправлен в `01-04` и `01-05` |

### Обоснования `reject`

1. Замечание 4 отклоняется как претензия к исполнителю: `src` он не менял, а изменённый файл —
   принятая ранее правка `01-01`. Неточность формулировки критерия в постановке — моя.

### Отложено (`defer`)

Нет.

## Проверка качества кода

- [x] Тесты детерминированы, сети нет, ничего не мокается зря (`NSubstitute` только для
      `IOptionsMonitor<WeatherSettings>` — это оправданно).
- [x] Границы проверяются по обе стороны порогов, а не только «серединка».
- [x] Ассерты осмысленные, проверяют формат вывода, а не реализацию.
- [x] Продакшн-код не изменён: `src/**` в этой задаче не тронут.
- [ ] Зависимости: лишняя `xunit.v3.extensibility.core` — см. замечание 1.
- [ ] Правки файлов вне разрешённых — замечание 2.
- [ ] Точность ассертов — замечание 3.

## Оценка отчёта исполнителя

**Отчёт заметно лучше предыдущего.** Ключевое: отклонения заявлены явно (§5), а не спрятаны —
именно так и должно быть. Раздел «Использованные скиллы» присутствует.

Три сценария были исправлены после первого прогона — то есть исполнитель **действительно
запускал тесты по ходу**, как требовала постановка, а не писал всё вслепую. Это ровно то
поведение, которого не хватило в `01-02`.

Оговорка по §8: «скиллы не использовались» — корректно, потому что **в этой постановке я их
не называл**. Это моя недоработка при разбивке задачи; в `01-04` и `01-05` блок со скиллами
добавлен.

## Повторное ревью (после fixes)

Отчёт: `tests-mapper-and-formatter_report_v2.md`. Проверено по коду, не по отчёту:

| Замечание | Что проверено | Итог |
|---|---|---|
| 1. Лишняя зависимость и `[Xunit.*]` | поиск `extensibility` по всем `*.props`/`*.csproj` | **нет совпадений** — убрана полностью ✅ |
| 1. Обычные атрибуты | маркеры в тестах: `using Xunit;`, `[Fact]`, `[Theory]`, `[InlineData(` | присутствуют; `[Xunit.*]` и `using Xunit.v3` — нет ✅ |
| 2. `because`-аргумент | поиск `"OrdinalIgnoreCase"` | **нет совпадений** — убран из всех вызовов ✅ |
| Приёмка | сборка и прогон тестов | `Ошибок: 0`, `Предупреждений: 0`, 68 пройдено, `сбой: 0` ✅ |

**Диагноз подтвердился:** исполнитель сам написал в отчёте, что зависимость «действительно была
лишней» — `using Xunit;` с обычными атрибутами работает на `xunit.v3` 4.0.0 и .NET 10, `CS0246`
не возникла. То есть первичная ошибка была именно неверным диагнозом, а не свойством окружения.

## Что дальше

1. Внести правки из `tests-mapper-and-formatter.fixes.md` (две правки по коду).
2. **`NoWarn S4144` — отклонено пользователем, устранено архитектором** (2026-09-22).
   Отключать правило анализатора ради дублирования тел недопустимо. Что сделано:
   `S4144` убран из `tests/Directory.Build.props`; три метода в
   `OpenMeteoWeatherMapperTests.cs` (`ToWindDirection_boundaryValues_returnsCorrectDirection`,
   `ToWindDirection_outOfRange_returnsUnknown`,
   `ToPrecipitationType_precipitationCodesWithZeroMeasurements_returnsNone`) имели **буквально
   совпадающие тела** с соседними теориями и слиты в них:
   `ToWindDirection_returnsExpectedDirection` и `ToPrecipitationType_returnsExpectedType`.
   Покрытие сохранено — все строки `InlineData` на месте, число тестов не изменилось: **68**.

   Проверка после правки: сборка `Ошибок: 0`, `Предупреждений: 0`, `S4144` не срабатывает,
   `WeatherBot.UnitTests.dll` — `всего: 68, сбой: 0`.
3. После этого — `01-04-tests-provider-digest-di`.
