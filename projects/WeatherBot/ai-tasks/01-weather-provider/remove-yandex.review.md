# Ревью: 01-01-remove-yandex

**Постановка:** `ai-tasks/01-weather-provider/01-01-remove-yandex.md`
**Отчёты исполнителя:** `remove-yandex_report_v1.md`, `remove-yandex_report_v2.md`
**Дата:** 2026-09-21
**Ревьюер:** архитектор

## Вердикт

**ОК (после доработок).** Первая итерация дала блокирующий дефект — потерянную регистрацию
`IHttpClientFactory` — и два нарушения контракта §5. Все четыре правки из
`remove-yandex.fixes.md` внесены и **проверены по коду и прогонам**, а не по отчёту.
Приёмка зелёная. Остались три незначительных замечания, отложенных в `01-02`.

## Прогоны (финальные, после fixes)

| Проверка | Команда | Результат |
|----------|---------|-----------|
| сборка | `dotnet build WeatherBot.slnx -c Debug -m:1 --no-restore -p:NuGetAudit=false` | `Ошибок: 0`, `Предупреждений: 0` ✅ |
| следы Яндекса | обход `*.cs,*.json,*.csproj,*.props,*.slnx,*.config` + `Select-String 'yandex\|яндекс'` (без `bin`/`obj`) | пусто ✅ |
| регистрация фабрики | поиск `AddHttpClient` по `src/**` | найдено: `DependencyInjectionExtension.cs:38` ✅ |
| литерал смещения | поиск `10800` по `src/**` | не найдено ✅ |
| тесты | — | тестовых проектов нет (задача `01-02`), прогонять нечего |
| git — чужие `.md` | `git status --short` + времена правки | исполнитель `.md`-артефакты **не трогал** ✅ |

**Как проверено, что `.md` не тронуты.** Дерево грязное не только от исполнителя: перед стартом
задачи я сам правил документацию, и её правки остались незакоммиченными. Поэтому «git показывает
изменённые `ARCHITECTURE.md`» ничего не доказывает. Взял времена правки: документы —
**06:25–06:26** (мои правки, до задачи), код — **06:34–07:06** (исполнитель). Расхождение
однозначное.

## Чекпоинты «требовалось → сделано»

| # | Требование постановки | Факт (файл) | Статус |
|---|-----------------------|-------------|--------|
| 1 | `WeatherSettings`: секция `OpenMeteo`, убрать `ApiKey`, добавить `City` | `WeatherSettings.cs` | ✅ |
| 2 | Создать `OpenMeteoContracts.cs` | создан; поле `UtcOffsetSeconds` добавлено на втором проходе | ✅ |
| 3 | Создать `OpenMeteoWeatherMapper.cs` по таблицам §5 | создан; правило нулевых осадков исправлено на втором проходе | ✅ |
| 4 | Создать `OpenMeteoWeatherProvider.cs` | создан; смещение берётся из ответа, разбор времени защищён | ✅ |
| 5 | Удалить три яндексовых файла | удалены | ✅ |
| 6 | Форматтер: город из настроек, время из `ObservedAt`, без «Москва»/«МСК» | `WeatherMessageFormatter.cs` | ✅ |
| 7 | DI: провайдер получает `IHttpClientFactory`, регистрируется синглтоном | `AddHttpClient()` на стр. 38, провайдер — `AddSingleton` на стр. 41 | ✅ |
| 8 | `appsettings.json`: `OpenMeteo` с `Latitude`/`Longitude`/`City`, без `ApiKey` | соответствует | ✅ |
| 9 | `BotController.cs` — комментарий без вендора | строка удалена, поведение не тронуто | ✅ |
| 10 | `WeatherBot.Domain.csproj` — комментарий секций | `YandexWeather` → `OpenMeteo` | ✅ |
| 11 | Сборка `Ошибок: 0`, `Предупреждений: 0` | подтверждено прогоном | ✅ |
| 12 | Поиск `yandex\|яндекс` пуст | подтверждено прогоном | ✅ |
| 13 | Продакшн-код вне объёма не менялся | `Application/**`, `WeatherInfo`, enum-ы, Telegram, фоновая задача — без изменений | ✅ |
| 14 | Секретов в коде и отчёте нет | `appsettings.Development.json` не тронут | ✅ |

## Замечания и их закрытие

| # | Замечание (файл:строка) | Важность | Решение | Статус |
|---|-------------------------|----------|---------|--------|
| 1 | `DependencyInjectionExtension.cs` — `IWeatherProvider` регистрируется синглтоном, но `AddHttpClient` во всём `src/**` отсутствует → резолв падает | блокирующее | `fix` | ✅ закрыто, проверено поиском |
| 2 | `OpenMeteoWeatherProvider.cs:104` — смещение UTC захардкожено `10800` | блокирующее | `fix` | ✅ закрыто, литерал отсутствует |
| 3 | `OpenMeteoWeatherMapper.cs:62` — правило «нулевых осадков» проверяло `weatherCode` вместо `precipitation` | значительное | `fix` | ✅ закрыто, `precipitation` в сигнатуре |
| 4 | `OpenMeteoWeatherMapper.cs:93` — незащищённый `DateTime.Parse`, исключение покидало инфраструктуру | значительное | `fix` | ✅ закрыто, `TryParseObservedAt` + `Result.Failure` |
| 5 | `01-01-remove-yandex.md` §7 — приёмка не проверяла резолв зависимостей | значительное | `fix` | ✅ закрыто архитектором: в §7 добавлена проверка `AddHttpClient`, в `01-02` — обязательный тест на композицию DI |
| 6 | `WeatherMessageFormatter.cs:66` — `(int)offset.TotalHours` обрезает смещение (для зон `:30`/`:45`) | незначительное | `defer` | `01-02` |
| 7 | `OpenMeteoWeatherMapper.cs:87` — отрицательное `precipitation` попадает в `_` → `VeryHeavy` | незначительное | `defer` | `01-02` |
| 8 | `OpenMeteoWeatherProvider.cs:96` — `(int)c.RelativeHumidity2m` усекает, а не округляет | незначительное | `defer` | `01-02` |

### Обоснования `reject`

Нет.

### Отложено (`defer`)

1. Замечания 6–8 — граничные случаи маппинга и форматирования; закрывать вместе с тестами
   в `01-02`, где эти границы и будут покрываться.

## Проверка качества кода

- [x] Слои и направление зависимостей не нарушены (`Web → Infrastructure → Application → Domain`).
- [x] Паттерны проекта: Result Pattern; абстракции в Application, реализации в Infrastructure;
      Domain — только BCL.
- [x] Ошибки не покидают инфраструктуру: `HttpRequestException`, `TaskCanceledException`,
      `JsonException` и теперь неразбираемое время — всё через `Result.Failure`.
- [x] Нет следов БД, ORM, миграций, фронтенда и Docker.
- [x] Нет дублей и magic strings: литерала смещения больше нет.
- [x] Секретов в коде нет; ключа погоды больше не существует как понятия.
- [x] Сетевые вызовы: таймаут 30 с, клиент из зарегистрированной фабрики.

## Оценка отчётов исполнителя

Оба отчёта хорошего качества и честные. В `v1` перечислены восемь ошибок компиляции и
анализаторов по ходу работы с точными кодами; в `v2` — по каждой правке указано, что и где
изменено. Заявленные «сборка 0/0» и «`yandex` не найден» подтверждены моими прогонами.

**По транскрипту сессии** (не по отчёту): исполнитель работал отдельной сессией DSH, провайдер
`qwen`, модель `unsloth/Qwen3.6-35B-A3B-MTP-GGUF`; 114 вызовов инструментов
(44 `read`, 20 `write`, 18 `edit`, 14 `todo_write`, 14 `pwsh`, 4 `grep`). Все пять файлов из
обязательного списка §2 постановки **прочитаны**: `memory-bank/snapshot.md`,
`memory-bank/activeContext.md`, `ARCHITECTURE.md`, `WORKFLOW.md`, `.dsh/AGENTS.md`.
Скиллы не загружались — для этой задачи они и не требовались.

## Что дальше

1. `01-01` считается принятой. Задача `01-02` разблокирована.
2. В `01-02` обязательны: тест на композицию DI, тесты границ маппинга, включая замечания 6–8.
3. Там же — проверка, загружает ли исполнитель названные в постановке официальные скиллы.
